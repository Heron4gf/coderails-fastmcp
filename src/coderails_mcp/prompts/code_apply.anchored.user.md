File path: {file_path}

The following is the definition of `{symbol}` (lines {start_line}-{end_line} of that file). Rewrite ONLY this definition according to the instruction; do not output anything outside it.

```
{content}
```

Edit instruction:
{instruction}
