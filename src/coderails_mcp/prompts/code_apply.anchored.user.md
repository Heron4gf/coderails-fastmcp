File path: {file_path}

The following is the definition of `{symbol}` (lines {start_line}-{end_line} of that file), between the <file> markers. Rewrite ONLY this definition according to the instruction.

<file>
{content}
</file>

Edit instruction:
{instruction}
